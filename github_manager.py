class GitHubManager(object):
	"""docstring for GitManager"""
	def __init__(self, git_user_name, token_file):
		self.git_user_name = user_name
		self.token_file = token_file
		self.url_repo = url_repo
		self.token = self._get_token()


	def _get_token(self):
		return 

	def init_git(self):
		return 
	
	def git_push_remote(self):
		return